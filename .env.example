# ===========================================
# API Doc Manager - 환경변수 설정
# ===========================================
# 이 파일을 복사하여 .env 파일을 만드세요:
# cp .env.example .env

# -------------------------------------------
# MongoDB 연결
# -------------------------------------------
# 로컬 개발용
MONGODB_URI=mongodb://localhost:27017/api-doc-manager

# MongoDB Atlas 사용시 (프로덕션)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# -------------------------------------------
# CORS 설정
# -------------------------------------------
# 허용할 도메인 (쉼표로 여러 도메인 구분)
CORS_ORIGIN=http://localhost:3000

# 프로덕션 예시
# CORS_ORIGIN=https://your-app.onrender.com,https://your-domain.com

# -------------------------------------------
# Slack 알림 설정 (선택사항)
# -------------------------------------------
SLACK_ENABLED=false
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
BASE_URL=http://localhost:3000
